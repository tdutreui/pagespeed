<%= form_with(model: lighthouse_report, local: true) do |form| %>
  <% if lighthouse_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lighthouse_report.errors.count, "error") %> prohibited this lighthouse_report from being saved:</h2>

      <ul>
        <% lighthouse_report.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
